# Phase 1: 가격 수집 + TCO 모델 + 정적 대시보드

| 항목 | 내용 |
|---|---|
| 상태 | **진행 중**: Task 1~7 완료, Task 8 코드 완료(테스트 30개). 수동 가격 10행 확인 완료(2026-09-11). **사용자 요청으로 중간 검토 체크포인트(Task 8b)를 설계했고, 승인을 기다린다.** 실제 실행은 Task 8b 이후에 한다 |
| 목표 | 로컬에서 플랫폼 4개 × 리전 2개의 가격을 모으고, 워크로드 3개의 월 비용을 추정한다. 사용자가 중간 결과를 판단해 컨펌하면 `site/index.html`을 만든다 |
| 선행 조건 | Phase 0 완료 |
| 코드 단위 계획 | `docs/plans/phase1-tco-dashboard.md` (테스트와 구현 코드 전문, 맨 위에 계획 이후 변경점과 Task 8b 설계) |
| 진행 규칙 | Task는 하나씩 진행하고, 끝나면 보고한 뒤 지시를 기다린다 (CLAUDE.md 규칙 10) |

## 이 Phase에서 답하려는 질문
- **T1:** 워크로드마다 월 비용 1위 플랫폼이 다른가? 가정을 ±50% 흔들어도 1위가 그대로인가? 미국과 서울을 모두 판정한다.
- **T2:** 서울 리전 추가 비용(서울 프리미엄)이 서비스마다 다른가? 기준은 최대값과 최소값의 차이가 10%p 이상인가이다.

## 데이터 흐름 (Task 8b 체크포인트 반영 예정)
```
[1단계] pipeline.py
AWS 가격 파일 ──┐
Azure API ──────┼─→ PriceRecord → data/raw/<날짜>/*.csv → model/tco.py → data/raw/<날짜>/review.md → [멈춤]
수동 가격표 ────┘   (세 수집기가 모두 성공한 뒤에만 저장)                    (검토 보고서)
                                                                                   │
                                                              [사용자가 판단 → 컨펌 / 반려]
                                                                                   │
[2단계] pipeline.py --confirm <날짜>  →  publish/render_site.py → site/index.html + approved.txt → 스냅샷 커밋
```

| 플랫폼 | 가격 출처 | 방식 | US / 서울 | 확인 상태 |
|---|---|---|---|---|
| Redshift | AWS 가격 파일 | 자동 | RPU-시간 $0.375 / $0.438, 스토리지 GB-월 $0.024 / $0.0261 | Task 2 실제 조회 [확인] |
| Databricks | Azure Retail API | 자동 | 서버리스 SQL DBU $0.70 / $0.95, ADLS GB-월 $0.0208 / $0.02 | Task 3 실제 조회 [확인] |
| Snowflake | 서비스 소비표 PDF | 수동 | Enterprise 크레딧 $3.00 / $4.05, 스토리지 TB-월 $23 / $25 | **사용자 확인 2026-09-11** (4행 전부) |
| BigQuery 컴퓨트 | 가격 페이지 | 수동 | Enterprise 슬롯-시간(종량제) $0.06 / $0.0765, 온디맨드 TiB $6.25 / $7.50 | **사용자 확인 2026-09-11** (4행 전부) |
| BigQuery 스토리지 | 가격 페이지 | 수동 | Active physical GiB-월 $0.04 / $0.052 | **사용자 확인 2026-09-11** (2행 전부. 월 기준 표시값도 일치) |

**BigQuery 가격표 참고 사항**
- **스토리지:** 가격 페이지는 GiB-시간과 GiB-월 두 가지로 표시한다.
  - **물리(사용 중):** US(us) $0.000054795/시간 = $0.04/월, 서울 $0.000071233/시간 = $0.052/월. 사용자가 두 표시를 모두 확인했고, 730시간 환산과 일치한다.
  - **논리(이전 사용, 기록용):** US(us) $0.000027397 → $0.02, 서울 $0.000031507 → $0.023.
- **리전 이름:** 페이지의 리전 이름은 "US (us)"다. 매월 무료 구간(스토리지 10GiB, 온디맨드 쿼리 1TiB)은 TCO 모델에서 제외한다.
- **컴퓨트 가격이 있는 곳**

  | 가격표 행 | 페이지 섹션 | 볼 곳 | 미국 / 서울 |
  |---|---|---|---|
  | 온디맨드 | **주문형 컴퓨팅 가격 책정** | 쿼리(주문형) 행의 "1 tebibyte and above" | $6.25 / $7.50 per TiB |
  | 슬롯 | **용량 컴퓨팅 가격 책정** | **기본값**(종량제) 열 | $0.06 / $0.0765 per slot-hour |

- **용량 컴퓨팅의 약정 가격 (모델에서는 쓰지 않음)**
  - 미국: BigQuery CUD 1년 $0.054 / 3년 $0.048, 리소스 CUD 1년 $0.048 / 3년 $0.036
  - 서울: BigQuery CUD 1년 $0.06885 / 3년 $0.0612, 리소스 CUD 1년 $0.0612 / 3년 $0.0459

## 진행 과정
| Task | 작업 | 담당 | 산출물 | 완료 기준 | 상태 |
|---|---|---|---|---|---|
| 1 | 뼈대 + 공통 스키마 | C | `requirements.txt`, `pytest.ini`, `.gitignore`, `common/schema.py` | 테스트 3개 통과, 첫 커밋 | **완료** (`d5930d1`) |
| 2 | AWS Redshift 수집기 | C | `collectors/aws_prices.py`, 픽스처 | 테스트 3개 통과, 실제 조회값이 Phase 0 값과 같음 | **완료** (`0c5b6c9`) |
| 3 | Azure 수집기 (Databricks, ADLS) | C | `collectors/azure_prices.py`, 픽스처 | 테스트 3개 통과, 실제 조회값 확인 | **완료** (`4e2ef6c`) |
| 4 | 수동 가격표 + 확인 게이트 | C+U | `collectors/manual_prices.py`, `data/manual/*_prices.csv` | 테스트 4개 통과, 사용자가 `confirmed_on` 입력 | **완료** (`c8da16f`). 수동 가격 10행 모두 확인(2026-09-11) |
| 5 | 워크로드 가정 + 월 비용 계산 | C+U | `data/manual/workloads.yaml`, `model/tco.py`, `tests/conftest.py` | 테스트 7개 통과(W1 손계산 $1,550 포함), 가정 검토 | **완료** (`56e95fb`, 가정 검토 반영 `529fbc4`) |
| 6 | T1·T2 판정 | C | `config/thresholds.yaml`, 판정 함수 4개 | 테스트 6개 통과 | **완료** (`7126385`) |
| 7 | 정적 대시보드 (기능 확인용 임시 디자인) | C | `publish/render_site.py`, `templates/site/index.html.j2` | 테스트 2개 통과(T1 두 리전 표시 포함) | **완료** (`4e90917`, 템플릿 추적 수정 `88fdddc`) |
| 8 | 파이프라인 | C | `pipeline.py` | 테스트 2개 통과(누적 30개). 게이트에서 멈출 때 반쪽 스냅샷 없음 | **코드 완료** (`e1be955`) |
| **8b** | **중간 검토 체크포인트** (사용자 요청) | C+U | `publish/review_report.py`, `templates/review.md.j2`, `pipeline.py` 수정 | 테스트 33개 통과. 1단계 실제 실행 → 사용자 판단 → 컨펌 → 2단계 사이트 생성 | **설계 승인 대기** |

Task마다 순서는 같다: 테스트 작성 → 실패 확인 → 구현 → 통과 확인 → 로컬 커밋.

## 중간 검토 체크포인트 (Task 8b, 2026-09-11 사용자 요청)
**원칙:** 파이프라인은 중간 결과를 만든 뒤 멈춘다. 사용자가 판단해 컨펌해야 나머지가 진행된다.

| 단계 | 명령 | 하는 일 | 끝나면 |
|---|---|---|---|
| 1. 수집·검토 | `pipeline.py` (기본) | 수집 → 스냅샷 저장 → 월 비용·판정 계산 → 검토 보고서 `data/raw/<날짜>/review.md` 작성 | **멈춘다.** 사이트는 만들지 않는다 |
| (사용자 판단) | `review.md` 읽기 | 수집 가격, 이전 승인 스냅샷 대비 변화, 월 비용 순위, T1·T2 판정, 확인 체크리스트 | 컨펌 또는 반려 |
| 2. 반영 | `pipeline.py --confirm <날짜>` | 검토 보고서가 있는 스냅샷만 받아 사이트를 생성하고 `approved.txt`를 기록 → 스냅샷 커밋 | 완료 |
| 반려 시 | — | 그 날짜 스냅샷은 커밋하지 않고 삭제한다. 원인을 고친 뒤 1단계를 다시 실행 | — |

- **검토 보고서 내용:**
  1. 요약: 날짜, 수집 건수, 이전 대비 변화 건수
  2. 수집 가격 표(18행)
  3. 이전 승인 스냅샷 대비 변화. 첫 실행이면 "첫 스냅샷"
  4. 시나리오 × 리전별 월 비용 순위
  5. T1(미국·서울), T2 판정
  6. 확인 체크리스트
  7. 다음 명령
- **Phase 2와의 연결:** 같은 검토 보고서를 PR에 넣는다. PR 머지가 컨펌이다(Phase 2 진행계획 참고).

## 가정 검토 결과 (2026-09-11, 커밋 `529fbc4`)
사용자 요청에 따라 Claude가 검토했다. 기준은 "바꿀 값이 있으면 중단하고 반영한다"이다.

| 가정 | 판단 | 조치 |
|---|---|---|
| **스토리지 10TB의 기준** | **변경 필요.** Snowflake·Redshift·ADLS는 압축된 저장량으로 과금하는데, BigQuery의 active *logical* storage는 압축 전 저장량으로 과금한다. 같은 데이터에서 BigQuery 비용이 압축률만큼 과소평가된다 | 기준을 "압축(물리) 후 10TB"로 통일하고, BigQuery는 **active physical storage** 단가를 쓴다 |
| W3 스캔량 20TiB | 유지. 기준이 모호했다 | **논리(압축 전) 바이트** 기준으로 명시한다. BigQuery 온디맨드 과금 기준과 같다 |
| small 1단위 동등성 | 유지. 벤더의 크기 이름 기준이고 성능 동등성은 검증할 수 없다 | Task 6의 ±50% 민감도와 Phase 4 실측 보정으로 다룬다 |
| W1 220시간, W2 60시간 | 유지 | — |
| 제외 항목(송출, 무료 등급, 최소 과금, 약정) | 유지. 가정 공개 섹션에 명시한다 | — |

**변경의 영향** (미국 기준)
- BigQuery 스토리지: $186 → $373
- W3 1위가 BigQuery($311)에서 Redshift($360)로 바뀌었고, BigQuery는 $498로 3위가 됐다.

## T1·T2 판정 결과 (Task 6 미리보기)
- 기준: `config/thresholds.yaml` (±50%, 10%p)
- 가격: 실제 가격표 값(이제 전부 확인됨)
- 정식 판정은 실제 실행의 검토 보고서에서 한다.

| 판정 | 미국 | 서울 |
|---|---|---|
| **T1** 1위가 워크로드마다 다른가 | **기각**: W1·W2·W3 모두 Redshift. 세 시나리오 모두 **민감** | **기각**: W1·W2·W3 모두 Redshift. 세 시나리오 모두 **견고** |
| **T2** 서울 프리미엄이 서비스마다 다른가 | **지지**: 최대-최소 차이 39.5%p | (리전 비교 지표라 하나의 판정) |

**왜 미국은 민감하고 서울은 견고한가**
- **미국:** Snowflake의 용량 가정을 ×0.5로 하면 세 시나리오 모두에서 Redshift보다 싸진다(W1 $890 < $900, W2 $590 < $600, W3 $350 < $360).
- **서울:** Redshift 컴퓨트 프리미엄(+16.8%)이 Snowflake(+35.0%)보다 훨씬 낮다. 그래서 Snowflake를 ×0.5로 해도 Redshift를 넘지 못한다(W1 $1,141 > $1,032).
- **의미:** "어느 플랫폼이 싼가"는 가정뿐 아니라 **리전**에 따라서도 확실성이 달라진다.
- **반영:** 대시보드는 T1을 미국·서울 두 리전 모두 표시한다(Task 7).

**서울 프리미엄 (T2 근거, 공시 단가 기준)**

| 항목 | 프리미엄 |
|---|---|
| Databricks ADLS 스토리지 | −3.8% (서울이 더 쌈) |
| Snowflake 스토리지 | +8.7% |
| Redshift 관리형 스토리지 | +8.8% |
| Redshift 서버리스 RPU | +16.8% |
| BigQuery 온디맨드 | +20.0% |
| BigQuery Enterprise 슬롯 | +27.5% |
| BigQuery 물리 스토리지 | +30.0% |
| Snowflake Enterprise 크레딧 | +35.0% |
| Databricks 서버리스 SQL DBU | +35.7% |

## 대시보드 (Task 7) 구성과 디자인 방침
- **구성 (`site/index.html`)**
  - 가설 판정표: T1 미국, T1 서울, T2
  - 시나리오 3개의 막대 차트(미국·서울 비교)와 비용표
  - 서울 프리미엄 표
  - 가정 공개(스토리지는 압축 후 물리 기준으로 명시)
  - 가격 확인일
- **디자인:** 기능 확인용 **임시 디자인**이다. 공개 대시보드 디자인은 Phase 2-5a에서 사용자가 준 템플릿을 바탕으로 정한다. Phase 2에서는 대시보드를 만들기 전에 멈추고 보고한다.

## 사용자가 할 일 (시점별)
| 시점 | 할 일 |
|---|---|
| **지금** | Task 8b(중간 검토 체크포인트) 설계를 승인한다 |
| Task 8b 1단계 실행 후 | `data/raw/<날짜>/review.md`를 읽고 판단한다. 컨펌하면 2단계로 간다 |
| 2단계 후 | `site/index.html` 화면을 확인하고, 자동 수집한 가격 3개를 공식 페이지와 대조한다 → Phase 1 완료 |
| Phase 2-5a | 마음에 드는 대시보드 템플릿을 준다 |

## 핵심 가정 (결과를 좌우한다. `data/manual/workloads.yaml`)
- **small 1단위:** Snowflake 시간당 2크레딧 / Databricks 시간당 12 DBU / Redshift 8 RPU / BigQuery 100슬롯
- **시나리오:**
  - W1: small×1로 월 220시간
  - W2: small×2로 월 60시간
  - W3: 월 20TiB(논리 바이트) 스캔. BigQuery는 온디맨드로 과금하고, 나머지는 small×4가 시간당 2TiB를 스캔한다고 보고 10시간으로 계산한다
- **스토리지:** 압축(물리) 기준 10TB
- **제외 항목:** 네트워크 송출, 무료 등급, 최소 과금 단위, 약정 할인

## 위험과 대응
| 위험 | 대응 |
|---|---|
| 실제 가격이 Phase 0 확인값과 다름 | 실제 조회 단계에서 보고한다. 가격 변동은 Phase 2의 E1 이벤트 후보다. Task 2(AWS)와 Task 3(Azure) 모두 값이 같았다 |
| AWS 파일의 선결제 항목이 단가로 섞임 | usagetype 접미어를 정확히 맞추고, 함정을 넣은 테스트로 막는다. 실제 데이터에서도 걸러지는 것을 확인했다 |
| Azure의 0원 체험·POC SKU, 클래식 SKU, 스토리지 구간이 섞임 | 상품명·SKU명·구간 조건을 정확히 맞추고, 함정을 넣은 테스트로 막는다 |
| 수동 가격을 잘못 입력 | `confirmed_on`이 없으면 실행이 멈춘다. 실제 파이프라인에서 멈추는 것도 확인했다 |
| 게이트에서 멈출 때 반쪽 스냅샷이 남음 | 세 수집기가 모두 성공한 뒤에만 저장한다. 테스트와 실제 실행 모두 확인했다 |
| **사람이 확인하지 않은 결과가 화면·기록에 반영됨** | Task 8b 체크포인트를 둔다. 검토 보고서 → 사용자 컨펌 → 반영 순서다. 스냅샷 커밋도 컨펌 이후에 한다 |
| 플랫폼마다 스토리지 과금 기준이 다름(논리 vs 물리) | 압축(물리) 기준으로 통일했다(2026-09-11 가정 검토) |
| 단위 변환 실수(GiB-시간 → GiB-월, GB ↔ GiB, TB ↔ GB) | 730시간/월, 1GiB = 1.073741824GB, 1TB = 1000GB로 고정하고 테스트로 확인한다. BigQuery 물리 스토리지는 페이지의 월 기준 값과도 일치했다 |
| "8 RPU = small" 가정이 Redshift에 유리할 수 있음 | 미국에서는 T1이 "민감"으로 나와 이 가정의 영향이 드러났다. Phase 4에서 보정한다 |
| 테스트 픽스처 가격과 실제 가격이 다름 | 픽스처는 로직 검증용이다(주석 명시). 실제 판정은 검토 보고서로 확인한다 |
| `.gitignore` 규칙이 의도하지 않은 파일을 제외함 | 루트 전용 패턴(`/site/`)을 쓰고, 커밋 뒤 `git ls-files`로 확인한다(Task 7에서 발생·수정) |
| plotly 7.0 설치 (계획은 5.x 기준) | 필요한 API의 호환성을 확인했다. Task 7 렌더링에서도 정상 동작했다 |

## 완료 기준 (게이트)
- pytest 통과: 현재 30개, Task 8b 이후 33개
- 1단계 실제 실행으로 첫 스냅샷과 검토 보고서 생성
- 사용자가 검토 보고서를 컨펌 → 2단계로 `site/index.html` 생성 → 첫 스냅샷 커밋
- 사용자가 화면을 확인하고 가격 3개를 대조
- CLAUDE.md 갱신
- 위 항목이 끝나면 Phase 2 코드 단위 계획을 작성하고 승인을 받는다. 대시보드를 만들기 전(2-5a)에는 멈춘다

## 진행 기록
- **2026-09-11 Task 1 완료:** venv, git init, 공통 스키마를 만들었다. 테스트 3개가 통과했다. 첫 커밋 `d5930d1`은 이 저장소 전용 noreply 이메일로 했다.
- **2026-09-11 Phase별 진행계획 문서화:** 이 파일을 포함해 `docs/phases/` 6개 파일을 만들었다(커밋 `6e8c670`).
- **2026-09-11 Task 2 완료** (커밋 `0c5b6c9`): 테스트 누적 6개. 실제 AWS 조회값이 Phase 0 값과 같다. 선결제 항목이 걸러졌다.
- **2026-09-11 Task 3 완료** (커밋 `4e2ef6c`): 테스트 누적 9개. 실제 Azure 조회값이 Phase 0 값과 같다.
- **2026-09-11 Task 4 코드 완료** (커밋 `c8da16f`): 테스트 누적 13개. Snowflake 4행과 BigQuery 논리 스토리지 2행을 사용자가 확인했다.
- **2026-09-11 Task 5 코드 완료** (커밋 `56e95fb`): 테스트 누적 20개.
- **2026-09-11 사용자 확인과 가정 검토** (커밋 `529fbc4`): BigQuery 컴퓨트 4행 확인, 전용 새 GCP 프로젝트 결정, 스토리지를 물리 기준으로 통일.
- **2026-09-11 Task 6 완료** (커밋 `7126385`): 판정 함수 4개. 테스트 누적 26개. 미리보기에서 T1은 기각(미국 민감, 서울 견고), T2는 지지(39.5%p).
- **2026-09-11 Task 7 완료** (커밋 `4e90917`, `88fdddc`)
  - T1을 두 리전 모두 표시한다. 테스트 누적 28개.
  - `.gitignore`의 `site/`가 `templates/site/`까지 제외해서 템플릿이 빠진 사고를 `/site/`로 수정했다.
- **2026-09-11 Task 8 코드 완료** (커밋 `e1be955`)
  - `pipeline.py`를 만들었다. 테스트 누적 30개.
  - 세 수집기가 모두 성공한 뒤에만 스냅샷을 저장한다.
  - 실제 실행을 시도했더니 물리 스토리지 게이트에서 멈췄고, 반쪽 스냅샷은 남지 않았다.
- **2026-09-11 BigQuery 물리 스토리지 확인과 체크포인트 설계**
  - 사용자가 붙여 준 값($0.000054795/시간 = $0.04/월, $0.000071233/시간 = $0.052/월)이 후보값과 일치했다. `confirmed_on`을 기록해서 수동 가격 10행이 모두 확인됐다.
  - 사용자 요청: "중간 결과를 내가 판단하는 단계, 컨펌하면 나머지 진행"
    - Task 8b 체크포인트를 설계했다(검토 보고서 → 컨펌 → 반영).
    - Phase 2에서는 PR 머지가 곧 컨펌이고, Sheets 적재는 컨펌 이후로 옮긴다.
    - 설계 승인을 기다린다.
