# Phase 3: LinkedIn 카드뉴스

| 항목 | 내용 |
|---|---|
| 상태 | **3-1~3-7 완료** (2026-09-15, 테스트 117개). 3-2 개정본 확정(2026-09-14), 보완안 확정(2026-09-15, D20 마무리 안내 장). 다음은 3-8이다 |
| 목표 | 가격이 의미 있게 바뀐 날 **카드뉴스 이미지와 게시문을 자동으로 만든다.** 검토 PR에서 미리 보고, 머지하면 사이트에 게시된다. **게시 버튼은 사람이 누른다**(약관) |
| 선행 조건 | Phase 2 완료(끝남), 이 문서와 코드 계획 승인 |
| 디자인 계획 | `docs/plans/phase3-card-design.md` (규칙 13의 디자인 게이트) |
| 코드 단위 계획 | `docs/plans/phase3-linkedin-cardnews.md` (Task 1~10) |
| 확정 문안 | `docs/scripts/cards-script.md`, `docs/scripts/linkedin-post-script.md` (규칙 16) |

## 방식 결정 (2026-09-12)

**A안을 택한다. 카드와 게시문까지 자동으로 만들고, 업로드와 게시는 사용자가 직접 한다.**

기존에 추천했던 A'안(공식 공유 링크)은 카드뉴스와 맞지 않는다. `linkedin.com/sharing/share-offsite/?url=…`은 **URL 하나를 공유하는 작성 창**을 열 뿐이고, 로컬 이미지 여러 장을 붙이거나 게시문을 미리 채워 넣을 수 없다 [지식]. A'로 올리면 카드뉴스가 아니라 링크 미리보기 게시물이 된다. 아카이브 링크만 공유하고 싶을 때 보조 수단으로 남겨 둔다.

| 안 | 방식 | 판단 |
|---|---|---|
| **A (채택)** | 카드 이미지와 게시문을 자동 생성한다. 사용자가 LinkedIn에서 올리고 게시한다 | 약관 위험이 없고, 카드뉴스 요구사항을 충족한다 |
| A' | 공식 공유 링크로 아카이브 URL을 공유한다 | 이미지 여러 장을 붙일 수 없다. 보조 수단으로만 남긴다 |
| B | 공식 API로 게시한다(사용자가 매번 실행) | 회색지대다. 이미지 업로드까지 자동화하면 약관 3.1(26)에 더 가깝다. 60일마다 토큰을 재발급해야 한다 |
| C | LinkedIn 제외 | 요구사항과 맞지 않는다 |

## 카드뉴스 구성

카드는 **1080×1350(4:5)** 이다. 산출물은 PNG 여러 장과 같은 내용을 묶은 PDF 1개다. 이미지 게시(피드에서 바로 보임)와 문서 캐러셀 게시(넘겨 보기) 중에서 사용자가 그날 골라 쓴다.

| 종류 | 장수 | 구성 |
|---|---|---|
| 소개 (첫 게시물) | 5장 | 무엇을 하는가 → 왜 비교가 안 되는가 → **사람의 승인을 거치는 자동화** → 현재 결과 → 마무리 안내 |
| 정기 (가격 변동) | **5장 이상** | 표지 → 단가 변동 내역 → 플랫폼별 월 사용료(**미국 리전·서울 리전 병기**, D18) → 월 사용료 변동 내역 → (순위 변동) → 마무리 안내(D20) |

**장수를 고정하지 않는다**(규칙 18). 변동 항목을 전부 실을 때까지 장을 늘린다. `외 N건` 같은 생략 문구를 쓰지 않는다. 카드뉴스의 목적은 정보 전달이다.

| 상황 | 단가 | 월 비용 | 순위 | 장수 |
|---|---|---|---|---|
| 전형적인 날 | 1 | 3 | 0 | 5장 |
| 순위가 바뀐 날 | 1 | 3 | 2 | 6장 |
| 벤더가 가격표를 개편 | 8 | 12 | 4 | 9장 |

**`가정과 산출 근거` 장은 두지 않는다**(2026-09-12 사용자 지시, D16). 정보 전달이 최우선이다. 모델 추정치라는 고지는 차트 장의 각주와 게시문 본문에 남기고, 대시보드 주소는 모든 장의 하단에 있다.

**모든 카드뉴스는 마무리 안내 장으로 끝난다**(2026-09-15 사용자 결정, D20). 전체 비교와 산출 근거가 있는 대시보드 주소를 크게 싣는다. 게시문에도 같은 주소가 있다.

## 흐름

```
[매일 수집, collect.yml]  단가 변동 감지
   └ E1(월 비용 ±1% 또는 순위 변화) → 글 초안 + 카드 PNG·PDF + 게시문(linkedin.md)
                                    → 검토 PR (본문에 카드 그림과 게시문이 보인다) → [멈춤]
        [사용자가 PR에서 판단 — 셋 중 하나]
         ├ 전면 반려: PR 닫기 → 아무것도 진행되지 않는다
         ├ 문구 반려: revise-copy 라벨 → [revise.yml] 재작성 요청 Issue
         │                              → [Claude] /humanize-korean → 같은 PR 갱신 → 재확인
         │                              (상한 3회. 초과 시 사람이 직접 수정)
         └ 승인: PR 머지
                    │
        publish.yml → 대시보드 게시 + site/cards/<날짜>/ 게시 + 시트 적재
                    │
        [사용자가 폰·PC에서 카드를 받아 LinkedIn에 직접 게시한다]
```

**문구 반려 루프의 한계:** `/humanize-korean`은 Claude 스킬이라 GitHub Actions 안에서 실행되지 않는다. 워크플로가 맡는 범위는 반려 감지와 요청 생성까지이고, 재작성은 Claude가 수행한다. 사용자가 할 일은 라벨을 붙이는 것뿐이다.

## 진행 과정

| 단계 | 작업 | 담당 | 산출물 | 완료 기준 |
|---|---|---|---|---|
| 3-0 | 방식 결정(A안), 디자인 템플릿 수령 | U | `_workspace/card-template/rlvnt-carousel/` | 완료(2026-09-12) |
| ~~3-1~~ | **완료.** 로컬 Edge·CI Chrome 양쪽에서 1080×1350 PNG와 PDF를 구웠다. CI는 `fonts-noto-cjk` 설치가 필수다 [확인] | C | `publish/browser.py` | 완료(2026-09-12) |
| ~~**3-2**~~ | **완료.** 카드 스크립트 작성 (규칙 16). 소개 4장과 정기 카드의 확정 문안을 먼저 쓴다. `/humanize-korean`으로 점검한다 | C → U | `docs/scripts/cards-script.md`, `docs/scripts/linkedin-post-script.md` | 완료(2026-09-12 승인). 변경률 1.1%, 등급 A |
| ~~3-3~~ | **완료.** 카드 데이터와 게시문. 기록 `docs/tasks/phase3/task-3-card-data-and-post.md`. 커밋 `9f50492` | C | `publish/card_data.py`, `publish/render_linkedin.py` | 항목 수에 따라 장이 늘어난다. 숫자 검사와 **누락 검사**(규칙 18)를 통과한다 |
| ~~3-4~~ | **완료.** 카드 HTML 템플릿(디자인 적용)과 굽기. 기록 `docs/tasks/phase3/task-4-card-template.md`. 커밋 `88174ca`. 사용자 실물 확인 승인 | C | `templates/cards/cards.html.j2`, `publish/render_cards.py` | 외부 요청이 0건이다. **사용자가 카드 실물을 확인한다** |
| ~~3-5~~ | **완료.** 파이프라인 연결. 기록 `docs/tasks/phase3/task-5-pipeline-cards.md`. 커밋 `b2c8576` | C | `pipeline.py --cards` | E1인 날 카드와 게시문이 자동으로 생긴다. 굽기가 실패해도 검토 자료는 남는다 |
| ~~3-6~~ | **완료.** 워크플로 연결. 기록 `docs/tasks/phase3/task-6-workflow.md`. 커밋 `d72560e`. 시뮬레이션 PR #4로 CI 실측 | C | `collect.yml` | 검토 PR 본문에 카드가 보인다. 머지하면 `site/cards/`가 게시된다 |
| **3-7** | **완료** (2026-09-15, `5f8740f`, 테스트 117개). 기록 `docs/tasks/phase3/task-7-revise-loop.md`. **문구 반려 재작성 루프 (규칙 16).** `revise-copy` 라벨을 받으면 재작성 요청을 열고, `/humanize-korean`으로 다듬어 같은 PR을 갱신한다 | C | `.github/workflows/revise.yml`, `docs/scripts/revise-loop.md` | 라벨을 붙이면 재작성 요청이 열린다. 재작성 후 같은 PR이 갱신된다. 3회를 넘기면 멈춘다 |
| 3-8 | 세부 계획 `docs/tasks/phase3/task-8-intro-cards.md`(2026-09-15 승인, D21·D22). 로컬 구현·카드 실물 승인(테스트 127개), push 승인 대기. 소개 카드 구현 | C | `card_data.intro_cards` | 3-2에서 확정한 문안을 그대로 쓴다. 숫자가 최신 승인 스냅샷과 같다 |
| 3-9 | 종단 검증 | C | 검증 기록 | 시뮬레이션 → PR 확인 → 머지 → 게시까지 이어진다 |
| 3-10 | **첫 게시** | U | LinkedIn 게시물 | 사용자가 직접 게시한다. Claude는 게시하지 않는다 |

**3-2를 신설한 이유:** 문안을 확정하지 않은 채 템플릿을 먼저 만들면, 구어체나 어조가 어긋난 문장이 코드에 박힌 뒤에 발견된다. 문안을 먼저 확정하고 승인받은 다음에 자동화한다(규칙 16).

## 사용자가 할 일

1. 이 문서와 코드 계획 2개를 승인한다.
2. **3-2에서 카드 스크립트(문안)를 승인한다.**
3. 3-4에서 카드 실물을 보고 디자인을 확인한다.
4. 문구가 마음에 들지 않으면 검토 PR에 `revise-copy` 라벨을 붙인다. 사유를 PR 코멘트로 남기면 재작성에 반영된다.
5. 3-10에서 LinkedIn에 직접 게시한다.

## 위험

| 위험 | 대비 |
|---|---|
| CI 서버에 한글 글꼴이 없어 글자가 네모로 구워진다 | **실측 확인됨**(`fc-list :lang=ko` → 0) [확인]. 굽기는 실패하지 않고 두부 글자가 조용히 만들어진다. 카드를 굽는 워크플로에 `fonts-noto-cjk` 설치를 **반드시** 넣는다(3-6) |
| 브라우저를 찾지 못해 카드 생성이 실패한다 | 카드 생성 단계를 `continue-on-error`로 두어 검토 PR은 그대로 열린다. 카드는 부가물이다 |
| 브라우저 명령줄 옵션이 버전에 따라 다르다 | 3-1에서 두 가지 표기를 모두 시도하고 결과를 [확인]으로 적는다. 실패하면 멈추고 Playwright 안을 다시 여쭙는다 |
| 한글 제목이 카드 밖으로 넘친다 | 가장 긴 플랫폼 이름·시나리오 이름으로 확인한다. 장 종류마다 최대 글자 수를 정한다 |
| 템플릿 원본 파일이 공개 저장소에 올라간다 | `_workspace/`는 `.gitignore` 대상이다. 커밋 전에 `git status`로 확인한다 |
| 카드에 개인 정보가 남는다 | 좌측 상단은 프로젝트 이름만 넣는다. 실명과 이메일을 넣지 않는다(규칙 11) |
| 재작성 루프가 끝나지 않는다 | 상한 3회를 둔다. 초과하면 자동 재작성을 멈추고 사람이 직접 고친다 |
| 재작성이 숫자를 바꾼다 | 숫자와 자리표시자는 재작성 대상에서 제외한다. 기존 숫자 검사가 그대로 막는다 |

## 제약 (Phase 0에서 확인, 그대로 유효하다)

- LinkedIn **API 약관 3.1(26)**: API로 게시를 자동화하는 것을 금지한다 [확인].
- LinkedIn **사용자 약관 13번**(2025-11-03): 봇이나 무단 자동화로 게시물을 만드는 것을 금지한다 [확인].
- Aside 같은 브라우저 자동화로 로그인 세션을 조종해 게시하는 방식도 위 조항에 해당한다. 계정이 제한될 위험이 있어 **Claude는 구현하지 않는다.**
- 코드에 LinkedIn API 호출을 넣지 않는다. 자동화는 **이미지와 문구 생성까지**다.

## 완료 기준 (게이트)

- 카드와 게시문이 자동으로 만들어지고, 모든 숫자가 `events.json`과 일치한다.
- 검토 PR에서 카드를 미리 볼 수 있다.
- 머지 후 사이트에서 카드를 받을 수 있다.
- 사용자가 첫 게시를 완료한다.

## 진행 기록

- **2026-09-12:** 새 대화에서 Phase 3을 시작했다. 방식을 A안으로 정했고(A'는 이미지 여러 장을 붙일 수 없다), 굽는 도구는 설치된 브라우저 직접 호출로 정했다. 생성 시점은 수집 단계, 게시는 머지 이후로 정했다.
- **2026-09-12:** 사용자가 카드뉴스 템플릿(1080×1350 PNG 10장)을 줬다. 분석해 `docs/plans/phase3-card-design.md`를 썼다. 사진에 기댄 장이 여섯 개여서, 사진 자리는 데이터 블록으로 대체하기로 했다. 좌측 상단은 프로젝트 이름만 넣는다.
- **2026-09-12:** 진행계획(이 문서)과 코드 계획을 작성하고 승인을 요청했다.
- **2026-09-12:** 3-1(브라우저 굽기 실측)의 로컬 부분을 완료했다. Edge로 1080×1350 PNG와 장수만큼의 PDF가 나오고 한글이 정상 렌더링된다 [확인]. CI 실측은 push 승인을 기다린다.
- **2026-09-12:** 사용자가 시험 카드의 구어체를 지적했다. 규칙 16(스크립트 단계 + executive 보고체)을 신설하고 3-2 단계를 만들었다. 확정 문안을 작성해 `/humanize-korean` light 경로로 점검했다(변경률 1.1%, 등급 A, 게이트 수렴). 공개 문안에서는 `컨펌` 대신 `승인`을 쓴다.
- **2026-09-12:** 사용자 지시로 문구 반려 재작성 루프(3-7)를 추가했다. `revise-copy` 라벨이 반려 채널이고, 재작성은 Claude가 `/humanize-korean`으로 수행한다. 상한은 3회다.
- **2026-09-12:** 사용자가 `외 N건` 생략 문구를 지적했다. 규칙 18(정보 생략 금지)을 신설하고 D13을 폐기했다. 항목이 많으면 장을 늘려 전부 싣는다. 단가 7건으로 실측해 2장 분할과 쪽 표시를 확인했다 [확인].
- **2026-09-12:** 사용자가 문안과 CI 실측 push를 승인했다. **3-1과 3-2를 완료했다.**
  - CI 실측: Chrome 152와 Chromium 152가 미리 설치돼 있다. **한글 글꼴은 0개다** [확인]. 글꼴 설치 전에는 모든 한글이 두부로 구워졌고, `fonts-noto-cjk` 설치 후 정상 렌더링됐다.
  - 임시 워크플로 `probe-browser.yml`을 삭제했다.
  - 사용자 지시로 여기서 멈춘다. 다음은 3-3이다.
- **2026-09-14:** 카드에 서울 리전이 나오지 않는다는 지적을 받았다. 차트 장이 리전 하나만 고르는 구조적 누락이었다. **D18**로 차트 장에 미국 리전·서울 리전을 병기하기로 했다(사용자 승인). 스크립트를 개정해 `/humanize-korean`으로 점검했고(변경률 0.0%, 등급 A), 실제 승인 스냅샷 금액으로 구워 넘침이 없음을 확인했다. 서울 막대 색과 리드 문구 한 곳은 사용자 결정 대기다.
- **2026-09-14:** 시험 굽기 중 `publish/browser.py` 결함을 발견해 고쳤다. Edge가 먼저 종료하고 파일은 하위 프로세스가 뒤늦게 쓴다 [확인]. 파일이 다 쓰일 때까지 기다리도록 바꿨다. 테스트 62개.
- **2026-09-14:** 서울 리전 금액의 통화를 검토했다. **D19**로 달러 표기를 유지하고 차트 장 각주에 통화 기준을 밝히기로 했다(사용자 승인). 원화 환산은 환율 변동만으로 E1을 일으켜 변동 감지를 오염시킨다. `/humanize-korean` 점검(변경률 0.0%, 게이트 수렴)을 마쳤다. 시험 굽기에서 각주 마지막 음절이 홀로 떨어지는 조판 결함을 발견해, 모든 카드에 `word-break: keep-all`을 적용하도록 디자인 계획에 넣었다.
- **2026-09-14:** 사용자가 서울 리전 막대 색(`#A9C4FD` 연한 파랑)과 차트 장 리드 명확화(`월 사용료 변동 폭이 가장 큰 시나리오는 …입니다.`)를 승인했다. **3-2 개정본을 확정했다.** 긴 시나리오 이름으로도 구워 넘침이 없음을 확인했다. 각주와 하단 알약 사이 여백이 좁아진 점은 3-4 검수 항목으로 넘겼다.
- **2026-09-14:** 3-3 세부 계획 `docs/tasks/phase3/task-3-card-data-and-post.md`를 작성했다(규칙 15). 확정 스크립트와 기존 코드를 대조해 스크립트에 없는 값 5건(서비스 라벨, 단가 추가·삭제 표기, E1 기준 자리표시자, 게시문 `영향` 표현, 단가 자릿수)과 구성 결정 1건(마무리 안내 장)을 확인했다. 착수 전에 스크립트를 보완해 승인받는다(Step 0). 테스트 계획은 +17개(→ 79)다. 승인을 기다린다.
- **2026-09-15:** 사용자가 보완 문안(단가 표기 조정 포함)을 승인했다. **3-3을 완료했다**(`9f50492`, 테스트 62 → 79).
  - `publish/card_data.py`: 장 구성, 쪽 나눔, 대표 시나리오, `events["display"]` 기록, 누락·글자 수·숫자 검사. `publish/render_linkedin.py`: 정기 게시문, 3,000자 초과 시 중단.
  - 실제 승인 스냅샷(2026-09-11)에 시뮬레이션 변동을 넣어 5장과 게시문 371자를 만들었다. 차트 금액 8개가 Task 2 시험 카드와 같다 [확인]. 생략 문구 0건.
  - 순위 배너의 서열 문자열(44자) 크기와 마무리 안내 장 조판은 3-4에서 정한다.
- **2026-09-15:** 3-4 세부 계획을 작성했다. 원본 대조 결과 좌우 여백 48px, 하단 알약 높이 56px·그라데이션 테두리, 본문 세로 가운데 정렬, 제목 굵기 700·본문 400, 글꼴 스택에 Noto Sans CJK KR 추가를 제안했다. 레이아웃 검사(겹침·넘침·줄 수·한 음절 줄)를 코드로 강제한다. 테스트 계획 +13개(→ 92).
- **2026-09-15:** 3-4를 구현했다(`88174ca`, 테스트 92개). 겹침·넘침·줄 수·한 음절 줄을 브라우저 실측으로 검사한다. 실제 승인 스냅샷으로 전형적인 날 5장과 변동이 많은 날 10장을 구웠고 모두 통과했다. 한글 라벨 자간을 줄였다. 사용자 실물 확인을 기다린다.
- **2026-09-15:** 사용자가 카드 실물을 승인했다. **3-4 완료.** 표지 제목 굵기는 기본안(700)을 유지한다.
- **2026-09-15:** 3-5 세부 계획을 작성했다. 승인을 기다린다.
- **2026-09-15:** **3-5 완료**(`b2c8576`, 테스트 101개). 임시 폴더 종단 실측으로 E1 1단계(카드 5장, 38초), 글꼴 없음(검토 자료 유지, 사유 기록), `--cards` 재생성, 승인 뒤 사이트 복사를 확인했다. 상대 경로 굽기 결함을 고쳤다.
- **2026-09-15:** 3-6 세부 계획을 작성했다. 승인 관문은 세 번이다(계획, 검토 PR 본문 문안, push·시뮬레이션 실행). 3-5 회귀 결함 1건을 함께 고친다.
- **2026-09-15:** 3-6 계획이 승인됐다. 검토 PR 본문 스크립트를 작성하고 점검했다. 문안 승인을 기다린다.
- **2026-09-15:** 3-6을 로컬에서 구현했다(`d72560e`, 테스트 108개). push와 CI 시뮬레이션 실측은 사용자 승인 뒤 한다.
- **2026-09-15:** **3-6 완료.** push 뒤 CI 시뮬레이션에서 한글 글꼴 설치(18초), 카드 5장 굽기(1단계 10초), 레이아웃 검사 통과, 검토 PR 본문의 카드·게시문 표시를 확인했다. 게시 워크플로의 승인일 읽기도 고쳐졌다. 시뮬레이션 PR은 닫았다.
- **2026-09-15:** 3-7 세부 계획을 작성했다. 승인 관문은 세 번이다(계획, 새 문안, push·실측).
- **2026-09-15:** 3-7 계획 승인. Step 0 새 문안(검토 PR 5~7절, 재작성 요청 Issue)을 작성해 `/humanize-korean`으로 점검했다(변경률 0.0%). 문안 승인을 기다린다.
- **2026-09-15:** 3-7 새 문안 승인, 로컬 구현 완료(테스트 117개). `revise.yml`, 게시 보류 파일, 재작성 절차서(`docs/scripts/revise-loop.md`). push·CI 실측 승인을 기다린다.
- **2026-09-15:** **3-7 완료.** CI 실측: 라벨을 붙이면 요청 Issue·보류 파일·본문 경고가 생기고, 떼면 사라진다 [확인]. 시험 Issue #6, PR #5는 닫았다. 다음은 3-8(소개 카드)이다.
- **2026-09-15:** 3-8 세부 계획을 작성했다. 4장 문안 보완, 결정 D21(로컬 굽기)·D22(글꼴 목록에 Noto Sans KR)를 승인받는다.
- **2026-09-15:** 3-8 계획·D21·D22 승인. Step 0 소개 4장 보완 문안을 점검했다(변경률 0.0%). 문안 승인을 기다린다.
- **2026-09-15:** 3-8 로컬 구현 완료(테스트 127개). 소개 카드 5장을 굽고 사용자 실물 승인을 받았다(2장 제목 줄바꿈 반영). push 승인을 기다린다.
