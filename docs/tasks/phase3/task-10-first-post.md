# Phase 3 · Task 10: 첫 게시

| 항목 | 내용 |
|---|---|
| 상태 | **게시 준비 완료, 사용자 게시 대기** (2026-09-16, `025dd0d`, 테스트 134개). 게시 전 수정 2건(PDF 테두리선 D23, 소개 4장 순위 전환 D24)이 사이트에 반영됐다 |
| 상위 계획 | `docs/plans/phase3-linkedin-cardnews.md` Task 10, 결정 D7(A안)·D21, 규칙 4·9·11 |
| 담당 | **U (사용자가 LinkedIn에 직접 게시한다).** Claude는 LinkedIn에 접근하거나 게시하지 않는다. LinkedIn API 약관 3.1(26)과 사용자 약관 13번이 자동 게시를 금지한다 [확인] |

## 목표

소개 카드뉴스와 소개 게시문을 사용자가 LinkedIn에 직접 올린다. Claude는 게시 전 확인과 게시 후 기록을 맡는다.

## 게시 전 확인 (Claude, 2026-09-15) [확인]

| 항목 | 결과 |
|---|---|
| 보안 점검 후속 설정 | 워크플로 기본 토큰 권한 `read`, 액션 커밋 SHA 고정 요구 켜짐, main 규칙 `main-protect`(기본 브랜치, 삭제·강제 push 차단, 우회 주체 없음) 활성. Wikis·Projects 꺼짐, 머지 후 브랜치 자동 삭제 켜짐, 자동 머지 꺼짐 |
| 확인하지 못한 설정 | **Dependabot alerts는 API가 여전히 꺼짐으로 응답한다**(게시와 무관, 사용자 재확인). 이메일 비공개·커밋 코멘트·push 한도는 현재 토큰 권한으로 읽을 수 없다(사용자 화면 확인) |
| 소개 카드 최신성 | 최신 승인 스냅샷이 2026-09-11이고 카드도 이 스냅샷으로 구웠다. 카드 코드(`templates/cards`, `card_data.py`, `render_cards.py`)는 `cb2db9f` 이후 바뀌지 않았다. 다시 구울 필요가 없다 |
| 게시 주소 | `cards.pdf`, PNG 5장, `linkedin.md` 모두 HTTP 200. 게시된 PDF가 저장소 파일과 바이트 단위로 같다 |
| 게시문 | 숫자가 없는 고정 문안(스크립트 2절), 3,000자 이내, 해시태그 4개. 게시문의 `월 비용`과 카드의 `월 사용료`가 다른 것은 D17의 의도된 불일치다 |

## 사용자 게시 절차

| 단계 | 할 일 | 주소·참고 |
|---|---|---|
| 1 | 파일을 받는다. 넘겨 보는 문서로 올리려면 PDF, 사진으로 올리려면 PNG 5장 | `https://eugeejo-ui.github.io/consumption-insights/cards/intro/cards.pdf`, `…/01-cover.png` ~ `…/05-closing.png` |
| 2 | 게시문 전체를 복사한다 | `https://eugeejo-ui.github.io/consumption-insights/cards/intro/linkedin.md` |
| 3 | LinkedIn에서 새 게시물을 열고 문서(PDF) 또는 사진(PNG 5장, 01 → 05 순서)을 추가한다 | PDF 문서 게시는 문서 제목을 요구한다 [지식]. 새 문안을 만들지 않도록 확정된 표지 제목 `데이터 플랫폼 네 곳의 월 사용료 추적`을 쓴다(규칙 16) |
| 4 | 게시문을 붙여 넣고 미리보기에서 줄바꿈, 대시보드 주소, 해시태그를 확인한 뒤 게시한다 | — |
| 5 (선택) | 폰 브라우저에서 PNG를 열어 저장되는지 확인한다(3-9에서 넘긴 항목) | — |

## 게시 전 결함 수정: PDF 강조 문구 테두리선 (2026-09-15)

### 발견

사용자가 `cards.pdf`를 열었더니 그라데이션 강조 문구(`월 사용료 추적`, `승인`, `대시보드`, `비교되지 않습니다`)마다 **문구를 감싸는 상자 가장자리에 희미한 주황 실선**이 보였다. PNG에는 없다. PDF 안에서 스크롤하면 사라졌다 다시 나타난다.

### 원인 [확인]

- 강조 문구는 `background: linear-gradient(...)` + `background-clip: text` + `color: transparent`로 그린다(그라데이션 사각형을 글자 모양으로 오려 낸다).
- **PNG**는 브라우저가 픽셀로 바로 그리므로 문제가 없다.
- **PDF**로 저장하면 Chromium이 이 기법을 문구마다 "그라데이션 사각형 + 소프트 마스크 두 겹"으로 기록한다. PDF 내부 확인 결과:
  - 사각형이 강조 상자 전체를 채운다(예: 표지 `48 573 615 138 re f`).
  - 첫 번째 마스크는 상자 사각형을 반전한 것(`/TR {1 exch sub}`)이다.
  - 두 번째 마스크는 글자 모양이다.
  - 소개 카드 PDF에 `/SMask`가 11개 있다.
- 보기 프로그램이 첫 마스크의 가장자리를 부드럽게(안티에일리어싱) 그리면서 반전 값이 0이 되지 않는다. 그 결과 사각형 테두리가 한 픽셀 폭으로 비친다.
- 배율과 화면 조각에 따라 가장자리가 걸리는 픽셀이 달라서 나타났다 사라진다.

### 재현 [확인]

사용자의 보기 프로그램과 다른 렌더러(pdf.js 3.11.174, 헤드리스 Edge)로 표지 강조 상자 오른쪽 가장자리 열을 쟀다. 상자 높이 전체에서 배경 대비 색 편차의 평균을 비교했다.

| 배율 | 현재 PDF | 승인된 PNG 5장으로 조립한 시험 PDF |
|---|---|---|
| 1.37배 | 2.0 | 0.0 |
| 2.13배 | 1.7 | 0.0 |
| 3배 | 13.2 | 0.0 |

3배 확대 이미지에서 현재 PDF는 강조 문구를 감싸는 선이 보이고, 시험 PDF는 선이 없다. 시험 PDF는 5쪽, 273,919바이트, `/SMask` 0개, 이미지 5개다. 쪽 전체가 여백 없이 카드로 채워진다.

LinkedIn은 올린 PDF를 자체 변환기로 그려서 보여 준다. 그 변환기에서 선이 보이는지는 확인할 방법이 없다 [미확인]. 서로 다른 렌더러 두 곳에서 재현됐으므로 게시 전에 고친다.

### 결정 D23 (2026-09-15 사용자 승인, A안)

**카드 PDF는 굽기가 끝난 PNG를 한 쪽에 한 장씩 실어 만든다.** 카드 HTML을 PDF로 직접 인쇄하지 않는다.

| 안 | 내용 | 채택 |
|---|---|---|
| **A** | 승인 대상 PNG로 PDF 조립 | **채택.** PDF와 PNG의 모습이 같아지고, 원인(소프트 마스크)이 사라진다. CI에서 굽는 정기 카드도 같은 함수를 쓰므로 함께 해결된다 |
| B | 강조 문구를 단색으로 | 기각. 승인한 디자인이 바뀐다 |
| C | 강조 문구를 SVG 글자로 | 기각. 템플릿과 레이아웃 검사를 크게 고쳐야 한다 |

**대가**
- PDF 안의 글자를 선택하거나 검색할 수 없다. LinkedIn 문서 게시와는 관계없다.
- 크게 확대하면 선명도가 PNG 해상도(1080×1350)에서 멈춘다.

### 세부 계획

**목표:** `bake()`가 만드는 `cards.pdf`에 소프트 마스크가 없고, 쪽마다 그 장의 PNG가 여백 없이 실린다. 소개 카드를 다시 굽고 게시 주소에 반영한다.

| 파일 | 변경 |
|---|---|
| `publish/render_cards.py` | `pdf_pages_html(pngs)` 추가(쪽마다 PNG 한 장, `@page` 1080×1350px, 여백 0). `bake()`는 PNG를 모두 구운 뒤 이 HTML을 임시 폴더에 써서 `browser.print_pdf`로 굽는다. 쪽수 검사에 더해 **이미지 수 = 장수**, **`/SMask` 없음**을 검사하고 어긋나면 `ValueError` |
| `tests/test_render_cards.py` | 아래 테스트 |
| `data/cards/intro/cards.pdf` | 다시 굽는다. PNG·`cards.html`·`linkedin.md`는 바뀌지 않아야 한다 |
| 문서 | 이 파일(결과), `CLAUDE.md`(D23, 확인된 사실, 진행 로그), `docs/phases/phase-3-linkedin.md`, `docs/plans/phase3-linkedin-cardnews.md`(D23), `docs/plans/phase3-card-design.md`(PDF 산출 방식), `docs/design/cardnews/`(새 단계 문서와 README) |

**테스트 (130 → 132)**

| # | 테스트 | 확인 내용 |
|---|---|---|
| 1 | `test_pdf_pages_show_each_png_on_its_own_full_page` (신규, 브라우저 불필요) | PNG마다 `<img>` 하나가 순서대로 절대 파일 주소로 들어가고, `@page{size:1080px 1350px;margin:0}`이 있다 |
| 2 | `test_bake_prints_the_pdf_from_the_baked_pngs` (신규, 브라우저 호출을 대체) | `print_pdf`가 `cards.html`이 아니라 PNG 조립 HTML로 불린다. 이미지 수가 다르거나 `/SMask`가 있는 PDF가 나오면 `ValueError`로 멈춘다 |
| 3 | `test_bakes_every_card_and_passes_the_layout_check` (보강, 실제 굽기) | 실제로 구운 PDF에 `/SMask`가 없고 이미지 수가 장수와 같다. **수정 전 코드로 돌리면 실패해야 한다** |

**단계**
1. 테스트 3개를 먼저 쓰고, 수정 전 코드에서 실패하는 것을 확인한다.
2. `render_cards.py`를 고친다. 전체 테스트를 통과시킨다.
3. `pipeline.py --cards intro`로 소개 카드를 다시 굽는다. PNG 5장·`cards.html`·`linkedin.md`가 커밋된 파일과 같은지 대조한다.
4. 새 PDF를 pdf.js로 다시 재서 가장자리 편차가 0인지 확인한다.
5. 문서를 갱신하고 로컬 커밋한다. 개인정보 검사 후 **push 승인을 받는다**(규칙 4).
6. push 뒤 게시 워크플로가 끝나면 게시 주소의 PDF가 저장소 파일과 같은지 확인한다.

**위험**

| 위험 | 대응 |
|---|---|
| 헤드리스 브라우저가 임시 폴더(한글 사용자 경로)의 HTML에서 다른 폴더의 PNG를 못 불러 빈 쪽이 된다 | 이미지 수 검사가 막는다. 실제 굽기 테스트가 로컬·CI에서 확인한다 |
| CI 브라우저의 PNG에 알파 채널이 있어 이미지 마스크(`/SMask`)가 생긴다 | 로컬 PNG는 알파 없는 RGB다(색 형식 2) [확인]. CI에서 다르면 CI 실제 굽기 테스트가 실패해 드러난다 |
| 다시 구운 PNG가 미세하게 달라진다 | 3단계에서 바이트 대조. 다르면 멈추고 보고한다 |

**완료 기준:** 테스트 132개 통과. 소개 카드 PDF에 `/SMask` 0개, 이미지 5개, 가장자리 편차 0. PNG·게시문 변경 없음. 게시 주소 반영 확인.

### 실행 결과 (2026-09-15, 로컬)

**한 일**
1. 테스트 3개를 먼저 썼다. 수정 전 코드에서 3개 모두 실패했다. 실제 굽기 테스트는 7장인데 PDF 이미지가 10개였다(강조 문구용 내부 이미지 포함) [확인].
2. `publish/render_cards.py`
   - `pdf_pages_html(pngs)`를 추가했다.
   - `bake()`는 PNG를 모두 구운 뒤 이 HTML을 임시 폴더에 써서 PDF로 굽는다.
   - 쪽수, 이미지 수, `/SMask`를 검사한다.
3. 전체 테스트 132개 통과(137초).
4. `pipeline.py --cards intro`로 소개 카드를 다시 굽고 로컬 커밋했다(`a5af3a8`).

**실측 [확인]**

| 항목 | 이전 PDF | 새 PDF |
|---|---|---|
| 크기 | 309,834바이트 | 273,914바이트 |
| 쪽 / 이미지 / `/SMask` | 5 / 3 / 11 | 5 / 5 / 0 |
| PDF 제목 | `cards` | `cards` |
| 표지 강조 상자 오른쪽 가장자리 편차(1.37배 / 2.13배 / 3배) | 2.0 / 1.7 / 13.2 | 0.0 / 0.0 / 0.0 |

- PNG 5장은 커밋된 파일과 **바이트 단위로 같다**.
- `cards.html`·`linkedin.md`는 저장소 기준 변경이 없다. 작업 사본의 줄바꿈(CRLF)만 다르다.
- 레이아웃 검사 통과: 가장 좁은 여백 245px, 가장 오른쪽 글자 끝 1020/1032px. 이전 굽기와 같다.
- PDF 메타데이터에 로컬 경로와 이메일이 없다. 커밋 대상 코드 개인정보 검사 0건.

**계획과 달라진 점**

| 계획 | 실제 | 이유 |
|---|---|---|
| 조립 HTML은 `@page`와 `<img>`만 | `<title>cards</title>`을 넣었다. 테스트 1에 확인을 더했다 | 첫 굽기에서 PDF 제목이 임시 파일 이름 `pdf-pages.html`로 기록됐다 [확인]. 이전 PDF와 같은 제목을 유지한다 |

**막혔던 것**
- 앱 안 브라우저가 로컬 파일을 열지 못해, 헤드리스 Edge에서 pdf.js로 렌더링해 쟀다.
- `<head>` 안 스크립트가 `document.body`를 찾지 못해 측정이 멈췄다. 스크립트를 `<body>` 뒤로 옮겨 해결했다.
- 측정 파일은 `_workspace/seam-probe/`(커밋 제외)에 있다.

### push와 게시 확인 (2026-09-15) [확인]

- 사용자 승인 아래 `a5af3a8`·`a68ee15`를 push했다. 원격에 새 커밋이 없어 리베이스하지 않았다.
- 게시 워크플로(실행 34972846050): build·deploy 성공, `log`는 새 승인이 없어 건너뛰었다.
- 게시 주소 7개가 모두 HTTP 200이다. `cards.pdf`는 저장소 파일과 바이트 단위로 같다(273,914바이트, 이미지 5개, `/SMask` 0개). PNG 5장과 `linkedin.md`도 저장소와 같다.

**남은 것**
- CI의 실제 굽기 테스트(한글 글꼴 설치 후)는 다음 매일 수집 실행에서 새 PDF 경로로 돈다. CI 브라우저에서도 `/SMask`가 없는지 그때 확인한다.
- 확인이 끝나면 사용자가 PDF를 게시한다.

## 게시 전 수정 2: 소개 4장을 플랫폼별 현재 순위로 (2026-09-15)

### 지적

사용자가 소개 4장 `현재 결과`를 반려했다. 문구가 `T1 기각 · 미국 리전`, `T2 채택`으로 되어 있어, 가설이 무엇인지, 무엇을 왜 기각했는지 독자가 알 수 없다. **플랫폼별 현재 상황을 적는다. 이 장만 고친다.**

### 데이터 확인 (2026-09-11 승인 스냅샷) [확인]

| 시나리오 | 리전 | 월 사용료가 낮은 순서 |
|---|---|---|
| 소규모 BI 대시보드(W1) | 미국 리전 | Redshift $900 › Snowflake $1,550 › BigQuery $1,693 › Databricks $2,056 |
| | 서울 리전 | Redshift $1,032 › Snowflake $2,032 › BigQuery $2,167 › Databricks $2,708 |
| 야간 배치 ETL(W2) | 미국 리전 | Redshift $600 › Snowflake $950 › BigQuery $1,093 › Databricks $1,216 |
| | 서울 리전 | Redshift $681 › Snowflake $1,222 › BigQuery $1,402 › Databricks $1,568 |
| 비정기 대용량 탐색(W3) | 미국 리전 | Redshift $360 › Snowflake $470 › BigQuery $498 › Databricks $544 |
| | 서울 리전 | Redshift $401 › Snowflake $574 › BigQuery $634 › Databricks $656 |

**세 시나리오와 두 리전 모두 순서가 같다.** 대시보드의 시나리오 선택 첫 항목은 W1이다(`templates/site/index.html.j2`).

### 1차 초안과 반려 (2026-09-15)

1차 초안은 리드에 `세 시나리오와 두 리전 모두 순위가 같습니다.`와 `금액은 소규모 BI 대시보드 시나리오의 월 사용료입니다.`를 두었다. `/humanize-korean` run `2026-09-15-005`는 변경률 0.0%였고, 시험 굽기는 레이아웃 검사를 통과했다(가장 좁은 여백 171px).

사용자가 다시 반려했다.

| 지적 | 반영 |
|---|---|
| `세 시나리오`는 사전 정보가 없는 LinkedIn 독자에게 뜻이 전달되지 않는다. 가설과 시나리오를 카드에 넣으면 정보 전달을 방해하고 집중을 흐린다 | **가설과 시나리오를 카드에서 뺀다.** 4장은 플랫폼별 순위와 두 리전 금액만 싣는다 |
| 1장 리드 `같은 워크로드를 기준으로 월 사용료를 환산해 매일 비교합니다.`는 카드뉴스를 매일 올린다는 뜻으로 읽힐 수 있다 | `매일`을 뺀 표현으로 바꾼다 |
| 스크립트만 먼저 보고한다 | 시험 굽기는 스크립트 승인 뒤에 한다 |

1차 초안의 결정 항목(금액 범위 A/B, 리드 줄바꿈, 순서가 다른 날의 리드)은 리드에서 시나리오를 빼면서 사라졌다. 금액은 대시보드를 열면 처음 보이는 워크로드(W1)를 쓰되 카드에 이름을 싣지 않는다.

### 2차 초안 (스크립트, 승인 대기)

| 장 | 위치 | 현재 | 2차 초안 |
|---|---|---|---|
| 1장 | 리드 | 같은 워크로드를 기준으로 월 사용료를 환산해 매일 비교합니다. | 같은 워크로드를 기준으로 월 사용료를 환산해 비교합니다. |
| 4장 | 제목 | 현재 **결과** | 월 사용료 **현재 순위** |
| 4장 | 리드 | 월 사용료가 가장 낮은 플랫폼이 1위입니다. | 월 사용료가 낮은 순서입니다. |
| 4장 | 항목 | T1·T2 3개 | `{순위}위 {플랫폼}` / `미국 리전 {미국 금액} · 서울 리전 {서울 금액}` 4개 |
| 4장 | 각주 | 작성일, 모델 추정치 | 작성일, 미국 달러 기준(D19 문안), 모델 추정치 |
| 4장 | 비교 기준(각주 아래, 작은 글자) | 없음 | `비교 기준: 소형 컴퓨트 월 220시간 · 스토리지 10TB` |

**비교 기준 줄 (2026-09-15 사용자 지시):** 4장 아래에 작은 글자로 비교 기준만 적는다. 순위가 어떤 기준에서 나왔는지 확인시키는 용도라 크게 쓰지 않는다. 짧고 간결하게 쓴다.
- 값의 출처: `data/manual/workloads.yaml`의 W1(`size: 1`, `hours_per_month: 220`)과 `storage_gb: 10000`.
- `소형 컴퓨트`는 플랫폼별 Small 1단위(`capacity_per_small`)다.
- 모두 [가정]이며 대시보드 "가정 공개"에 있다.
- 조판안: 각주 아래 한 줄, 각주(26px)보다 작은 22px, 흐린 회색(`--faint`). 시험 굽기에서 확인한다.
- `/humanize-korean` run `2026-09-15-007`(light): 변경률 0.0%, 게이트 수렴. 값을 넣으면 33자다.
- 스킬 보고 1: `컴퓨트` → `컴퓨팅` 제안. 대시보드와 정기 카드가 `컴퓨트`를 쓰므로 유지를 권고한다.
- 스킬 보고 2: 저장량이 압축 후 기준인데 표시가 없어 압축 전으로 읽힐 수 있다. `스토리지 10TB(압축 후)`로 쓰면 38자다. 사용자 결정 사항이다.

### 같은 문제가 있는 곳 (사용자 결정 필요)

| # | 위치 | 현재 문안 | 문제 | 제안 |
|---|---|---|---|---|
| 1 | LinkedIn 게시문 첫 줄(`docs/scripts/linkedin-post-script.md` 2절) | 데이터 플랫폼 네 곳의 월 비용을 매일 추적합니다. | 1장 리드와 같다. 게시를 매일 한다는 뜻으로 읽힐 수 있다 | 데이터 플랫폼 네 곳의 월 비용을 추적합니다. |
| 2 | 마무리 안내 장 리드(소개·정기 공통, D20) | 시나리오별 월 사용료, 서울 프리미엄, 가정과 산출 근거를 공개합니다. | `시나리오별`, `서울 프리미엄`은 사전 정보가 없는 독자에게 뜻이 전달되지 않는다 | 워크로드별 월 사용료, 리전별 가격 차이, 계산 가정을 공개합니다. |
| 3 | 정기 카드(차트 장 리드, 월 사용료 변동 항목, 순위 변동 배너) | `{시나리오}` 이름을 싣는다 | 같은 문제다. 다만 어느 조합이 바뀌었는지 밝히려면 워크로드 구분이 필요하다 | 이번 수정 범위에서 뺀다. 첫 실제 변동 전에 따로 다룬다 |
| 4 | 4장 각주 1 | {작성일} 승인 스냅샷 기준입니다. | `승인 스냅샷`은 누가 무엇을 승인했는지 독자가 알 수 없다(`/humanize-korean` 보고) | {작성일} 가격 기준입니다. |
| 5 | 4장 각주 2, 정기 차트 장 각주(D19) | 금액은 벤더 공시 통화인 미국 달러 기준입니다. | `벤더 공시 통화`는 업계 용어다(같은 보고) | 금액은 모두 미국 달러 기준입니다. |
| 6 | 4장 각주 3, 정기 차트 장 각주 | 약정 할인을 제외한 모델 추정치입니다. | `모델`이 AI 모델로 읽힐 수 있다(같은 보고) | 약정 할인을 제외한 추정 금액입니다. |

`/humanize-korean` run `2026-09-15-006`(light): 2차 초안과 제안 문장 모두 변경률 0.0%, 구조 게이트 수렴. `가설`·`시나리오`·`매일` 0건. 기록은 `docs/scripts/cards-script.md` 5절 끝. 제안 5·6은 정기 카드와 같은 상수를 쓰므로 채택하면 정기 카드 각주도 함께 바뀐다.

### 시험 굽기 (2026-09-16) [확인]

저장소 코드를 고치지 않고 작업 폴더에서만 구웠다(`_workspace/trial-a`, `_workspace/trial-b`, 커밋 제외). 비교 기준 줄은 시험용으로 22px `.basis` 문단을 주입해 그렸다.

| 변형 | 4장 각주 | 비교 기준 줄 |
|---|---|---|
| 가 | 현재 문안(`승인 스냅샷`, `벤더 공시 통화`, `모델 추정치`) | `비교 기준: 소형 컴퓨트 월 220시간 · 스토리지 10TB` |
| 나 | 쉬운 문안(제안 3·4·5) | `… · 스토리지 10TB(압축 후)` |

- 두 변형 모두 5장 레이아웃 검사를 통과했다. 가장 좁은 여백 168px, 가장 오른쪽 글자 끝 1020/1032px이다.
- 비교 기준 줄은 각주 아래 한 줄로 들어가고, 각주보다 작아 눈에 띄지 않는다. 압축 후 표시를 넣어도 한 줄을 유지한다.
- 1장 리드에서 `매일`을 뺀 모습도 함께 구웠다. 줄 수는 그대로다.
- 사용자에게 이미지 3장을 보고했다. 1장은 승인받았다.
- **2026-09-16 사용자 직접 수정:** 4장 각주를 명사구로 줄였다(`…기준입니다.` → `…기준`, `…추정치입니다.` → `…추정치`). 사용자 지시로 컨펌 없이 바로 다시 구웠다(`_workspace/trial-c`). 5장 레이아웃 검사 통과, 가장 좁은 여백 168px.
- 비교 기준 줄은 `(압축 후)` 없이 구웠다. 표시 여부는 사용자 결정으로 남아 있다.

### 세부 계획 (2차 초안 승인 뒤 실행)

| 파일 | 변경 |
|---|---|
| `docs/scripts/cards-script.md` | 3절 1장 리드와 4장을 확정본으로 교체하고 점검 기록을 남긴다. 결정 2를 채택하면 1-1절도 바꾼다 |
| `docs/scripts/linkedin-post-script.md`, `templates/post_linkedin_intro.md.j2` | 결정 1을 채택하면 첫 줄을 바꾼다 |
| `publish/card_data.py` | 1장 리드, 4장 순위 항목. 가설 항목 함수(`_t1_item`, `_t2_item`, `VERDICT`)는 쓰는 곳이 없어져 지운다. 숫자 검사 기준 사전에 순위와 금액을 넣는다. W1의 두 리전 순서가 다르면 `ValueError`로 멈춘다. 결정 2를 채택하면 `closing_card()` 리드 |
| `pipeline.py` | `rebuild_intro()`가 계산 결과(`rows`)를 넘긴다 |
| `tests/test_card_data.py`, `tests/test_render_cards.py` | 가설 문구 테스트를 순위 문구 테스트로 바꾼다. 리전 순서가 다를 때 멈춤, 숫자 검사, 1장 리드에 `매일`이 없음 |
| `data/cards/intro/` | 다시 굽는다 |

**순서**
1. 2차 초안 스크립트와 `/humanize-korean` 점검을 보고하고 **스크립트 승인**을 받는다.
2. 시험 굽기로 레이아웃을 확인한다. 통과하면 3단계로 가고, 넘치면 멈추고 보고한다.
3. 테스트를 먼저 쓰고 구현한다.
4. 소개 카드를 다시 굽고 **카드 실물 승인**을 받는다.
5. 문서를 갱신하고 로컬 커밋한 뒤 **push 승인**을 받는다.
6. 게시 주소를 확인한다.

## 게시 후 기록 (Claude, 사용자가 알려 준 뒤)

- 게시 날짜, 올린 형식(PDF 문서 / 사진), 미리보기에서 고친 점이 있었는지를 이 파일과 `CLAUDE.md` 진행 로그에 적는다.
- **게시물 주소는 사용자가 원할 때만 적는다.** 주소에 개인 프로필 정보가 들어갈 수 있다(규칙 11).
- `CLAUDE.md` 3-10을 완료로 표시한다. Phase 3 게이트(첫 게시 확인)를 통과하면 Phase 4 상세 계획은 사용자 지시 뒤에 쓴다.

## 이후 과제로 이어지는 확인

| 항목 | 시점 | 위치 |
|---|---|---|
| 실제 승인 시 카드 게시와 시트 적재가 한 번만 일어나는지 등 10항목 | 첫 실제 가격 변동 | `docs/tasks/phase3/task-9-e2e.md` 끝 |
| 2026-09-15 날짜의 반려 회차: 닫힌 시험 요청 2개(#6, #8)가 회차에 들어간다 | 같은 날짜의 실제 검토 PR이 반려될 때(가능성 낮음) | 같은 파일 |
| main 이력의 시뮬레이션 커밋 3개(`c07fb5a`, `fbba8a3`, `f9eed6c`). 데이터 폴더는 지웠다 | 조치 없음 | 같은 파일 |
| Dependabot alerts 켜짐 확인 | 사용자 설정 뒤 | `docs/security/2026-09-15-pre-publish-audit.md` |

### 실행 결과 (2026-09-16)

**한 일**
1. 승인받은 문안을 스크립트 정본에 반영했다(`docs/scripts/cards-script.md` 1-1·3절, `docs/scripts/linkedin-post-script.md` 2절).
2. 테스트를 먼저 고치고 더했다. 수정 전 코드에서 실패하는 것을 확인했다.
3. `publish/card_data.py`
   - `rank_items(rows, scenario)`를 더했다. 첫 시나리오의 월 사용료로 순위를 만들고, 두 리전 순서가 다르면 `ValueError`로 멈춘다.
   - 4장을 순위 항목 4개 + 명사구 각주 3줄 + 비교 기준 줄로 바꿨다.
   - 1장 리드에서 `매일`을 뺐다. 마무리 안내 장 리드를 바꿨다.
   - 가설 항목 함수(`_t1_item`, `_t2_item`, `VERDICT`)를 지웠다.
   - 숫자 검사 기준 사전을 순위·금액·사용 시간·저장량으로 바꿨다.
4. `templates/cards/cards.html.j2`에 `.basis`(22px, 흐린 회색)를 더하고 각주 아래에 그린다.
5. `templates/post_linkedin_intro.md.j2` 첫 줄을 바꿨다.
6. `pipeline.py --cards intro`로 다시 구웠다.

**테스트 132 → 134**

| 테스트 | 확인 내용 |
|---|---|
| `test_intro_ranks_the_platforms_by_monthly_cost` (교체) | 제목·리드·항목 4개·명사구 각주·비교 기준 줄. 카드 글자에 `T1`·`T2`·`채택`·`기각`·`시나리오`가 없다 |
| `test_intro_stops_when_the_two_regions_rank_differently` (신규) | 두 리전 순서가 다르면 생성을 멈춘다 |
| `test_the_basis_line_is_smaller_than_the_notes` (신규) | 비교 기준 줄이 각주 아래에 한 줄로 들어가고 글자가 각주보다 작다. 다른 장에는 없다 |
| `test_intro_copy_matches_the_script` (보강) | 스크립트 3절의 항목·비교 기준 줄까지 글자 단위로 대조한다 |

**실측 [확인]**

| 항목 | 값 |
|---|---|
| 전체 테스트 | 134개 통과 |
| 레이아웃 검사 | 5장 통과. 가장 좁은 여백 168px, 가장 오른쪽 글자 끝 970/1032px |
| 다시 구운 파일 | 1·4·5장 PNG, `cards.pdf`, `cards.html`, `linkedin.md`. 2·3장 PNG는 바이트 단위로 같다 |
| PDF | 290,954바이트, 5쪽, 이미지 5개, `/SMask` 0개(D23 유지) |
| 게시문 첫 줄 | `데이터 플랫폼 네 곳의 월 비용을 추적합니다.` |

**계획과 달라진 점**

| 계획 | 실제 | 이유 |
|---|---|---|
| `pipeline.py`가 계산 결과를 넘기도록 고친다 | 고치지 않았다 | `analyze()` 결과에 `rows`와 `workloads`가 이미 들어 있다 |
| 4장만 바뀐다 | 1·5장도 바뀐다 | 1장 리드(`매일` 삭제)와 마무리 안내 장 리드를 함께 승인받았다 |

### push와 게시 확인 (2026-09-16) [확인]

- 사용자 승인 아래 `a8c19e4`·`025dd0d`를 push했다. 원격에 새 커밋이 없어 리베이스하지 않았다.
- 게시 워크플로(실행 35047188082): build·deploy 성공, `log`는 새 승인이 없어 건너뛰었다.
- 게시 주소 7개가 모두 HTTP 200이고, PDF·PNG 5장·게시문이 저장소 파일과 바이트 단위로 같다.
- 게시문 첫 줄이 `데이터 플랫폼 네 곳의 월 비용을 추적합니다.`로 바뀌었다.

**남은 것:** 사용자가 LinkedIn에 게시한다. 다음 매일 수집 실행에서 CI 실제 굽기 테스트(새 PDF 방식)를 확인한다.
