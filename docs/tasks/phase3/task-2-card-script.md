# Phase 3 · Task 2: 카드 스크립트 작성

| 항목 | 내용 |
|---|---|
| 상태 | **완료** (2026-09-12 사용자 승인) |
| 상위 계획 | `docs/plans/phase3-linkedin-cardnews.md` Task 2, 규칙 16 |
| 담당 | C (승인: U) |

## 목표

카드뉴스와 LinkedIn 게시문에 실릴 문장을 코드보다 먼저 확정한다. 어조는 executive 보고체다.

이 Task는 규칙 16과 함께 신설됐다. 신설 사유는 다음과 같다. 문안을 확정하지 않은 채 템플릿을 먼저 만들면, 어조가 어긋난 문장이 코드에 박힌 뒤에 발견된다. 실제로 Task 1의 시험 카드에 구어체 문장이 들어갔고 사용자가 이를 지적했다.

## 건드리는 파일

| 파일 | 동작 |
|---|---|
| `docs/scripts/cards-script.md` | 신규. 카드 확정 문안 |
| `docs/scripts/linkedin-post-script.md` | 신규. 게시문 확정 문안 |
| `_workspace/2026-09-12-002/` | humanize-korean 작업 폴더. 저장소에 커밋하지 않는다 |

## 단계

- [x] **Step 1** 초안 작성
- [x] **Step 2** `/humanize-korean` 점검
- [x] **Step 3** 확정 문안으로 정기 카드 5장을 구워 글자 넘침 확인
- [x] **Step 4** 사용자 승인 (2026-09-12)
- [x] **Step 5** 커밋

## 실행 결과

### humanize-korean 점검

| 항목 | 값 |
|---|---|
| run_id | `2026-09-12-002` |
| 경로 | light. 사전 점수 risk_band low(score 1), route_hint light |
| 변경률 | **1.1%** (게이트 확정값). 문장 터치율 11.8% (8/68) |
| 등급 | A. 자체검증 6/6 |
| 구조 게이트 | exit 0 수렴. 수치 주입·인용·register 이상 없음 |

고친 항목 7건과 유지 근거는 `docs/scripts/cards-script.md` 5절에 있다.

**스킬 실행 환경 정정 [확인]:** 플러그인 루트는 `C:\im-not-ai`이고 `scripts/`가 그 아래에 있다. `CLAUDE.md`에 있던 "스크립트 경로를 찾지 못한다"는 기록은 현재 환경과 맞지 않아 정정했다. 정량 점수 shim과 구조 게이트를 모두 사용했다.

**용어 결정:** 공개 문안에서는 `승인`을 쓴다. `컨펌`은 외래어 구어체다. 내부 문서와 규칙 14의 `컨펌`은 그대로 둔다.

### 규칙 18 반영 (2026-09-12, 사용자 지적)

초안에는 `외 {생략건수}건은 대시보드에 있습니다`라는 생략 문구가 있었다. 사용자가 정보 전달이라는 목적에 어긋난다고 지적했다.

**조치:** 규칙 18을 신설하고 생략 문구를 전면 폐기했다. 항목이 많으면 같은 종류의 장을 늘려 전부 싣는다. 결정 D13을 폐기하고 D15로 대체했다.

| 장 | 한 장에 담는 항목 수 |
|---|---|
| 단가 변동 내역 | 5 |
| 월 비용 영향(대표) | 4 |
| 월 비용 변동(이어서) | 6 |
| 순위 변동 | 배너 2 |

같은 종류가 2장 이상이면 제목에 `(1/2)`를 붙인다. 카드에 실린 항목 수가 `events.json`의 항목 수와 다르면 생성을 중단한다.

**실측:** 단가 변동 7건을 넣어 구웠더니 단가 장이 2장(`(1/2)`, `(2/2)`)으로 늘고 7건이 모두 실렸다 [확인]. 생략 문구는 없다.

### 확정 문안으로 구운 결과

정기 카드 5장을 확정 문안으로 구웠다. 모두 1080×1350이고 PDF는 5쪽이다.

| 파일 | 장 | 크기 |
|---|---|---|
| `01-cover.png` | 표지 | 48KB |
| `02-prices.png` | 단가 변동 내역 (1/2) | 88KB |
| `03-prices.png` | 단가 변동 내역 (2/2) | 56KB |
| `04-chart.png` | 월 비용 영향 | 71KB |
| `05-closing.png` | 가정과 산출 근거 | 54KB |

**글자 넘침 없음.** 4절 상한(표지 10자, 본문 제목 14자)이 실제로 지켜진다.

**Task 4로 넘기는 것:** 내용이 적은 장(표지, 단가 변동 내역)은 아래쪽 여백이 크다. 본문 블록의 수직 위치를 내용량에 따라 조정해야 한다. 원본 템플릿도 여백이 넉넉하므로 균형만 맞춘다.

### D16 반영 (2026-09-12, 사용자 지시)

`가정과 산출 근거` 장을 배제했다. 정보 전달이 최우선이라는 판단이다.

- 정기 카드: 표지 → 단가 변동 → 월 비용 영향(차트) → 월 비용 변동(목록) → 순위 변동. 맺음 장이 없다.
- 소개 카드: 5장에서 **4장**으로 줄었다.
- 고지("약정 할인을 제외한 모델 추정치")는 **월 비용 장의 각주**와 **게시문 본문**에 남겼다. 장이 사라져도 수치가 실제 청구액으로 오해되지 않게 한다.
- 월 비용은 차트 장과 목록 장을 나눴다. 예전 안의 "대표 장에 4줄까지" 예외가 없어지고 장수 계산이 단순해졌다.

| 상황 | 단가 | 월 비용 | 순위 | 장수 |
|---|---|---|---|---|
| 전형적인 날 | 1 | 3 | 0 | 4장 |
| 순위가 바뀐 날 | 1 | 3 | 2 | 5장 |
| 벤더가 가격표를 개편 | 8 | 12 | 4 | 8장 |

**실측:** 전형적인 날 구성으로 구웠더니 4장이 나왔다(`01-cover`, `02-prices`, `03-chart`, `04-costs`). PDF는 4쪽이다. 월 비용 장의 각주에 고지가 들어가고, 마지막 장 하단에 `끝`이 표시된다 [확인].

### D17 반영 (2026-09-12, 사용자 지시)

카드 문안의 `월 비용`을 **`월 사용료`** 로 바꿨다. 네 플랫폼은 종량 과금이므로 '사용료'가 정확하고, 가격 비교 문맥에 맞는다.

| 위치 | 이전 | 현재 |
|---|---|---|
| 표지 제목 | 월 비용 순위 변동 / 월 비용 변동 | 월 사용료 순위 변동 / 월 사용료 변동 |
| 차트 장 | 월 비용 영향 | 월 사용료 영향 |
| 목록 장 | 월 비용 변동 | 월 사용료 **변동 내역** |
| 순위 장 리드 | 월 비용이 낮은 순서입니다 | 월 사용료가 낮은 순서입니다 |
| 소개 1·2·3·4장 | 월 비용 추적 / 월 비용 계산 / 월 비용 1위 | 월 사용료로 통일 |

목록 장 제목을 `변동 내역`으로 바꾼 이유가 있다. 표지 제목이 `월 사용료 변동`이라 목록 장과 글자가 겹쳤다. `단가 변동 내역`과 짝이 맞는다.

**의도된 불일치:** 대시보드 화면, 검토 보고서, 글 초안, LinkedIn 게시문, 규칙 17의 고정 용어는 아직 `월 비용`이다. 교체는 **다른 대화에서 진행한다**(사용자 지시). 카드와 대시보드의 용어가 한동안 다르다. 되돌리지 않는다.

**실측:** 새 용어로 4장을 다시 구웠다. `월 사용료`는 4자라 104px 표지에서 한 줄에 들어가고 강조 낱말 `변동`이 둘째 줄로 떨어진다. 글자 넘침은 없다 [확인].

### 차트 장 제목 변경 (2026-09-12, 사용자 지적)

`월 사용료 영향`이 무엇을 가리키는지 불분명하다는 지적을 받았다. 원인은 낱말이 아니라 **제목과 차트 내용의 불일치**였다. 차트는 변동 후 네 플랫폼의 절대 금액을 보여 주는데 제목은 변화량을 말하고 있었다.

**조치:** 제목을 차트 내용에 맞춰 `플랫폼별 **월 사용료**`로 바꿨다. 강조는 `월 사용료`에 준다.

사용자가 제안한 `월 사용료 변동 영향`은 채택하지 않았다. 세 가지 이유를 보고하고 동의를 받았다.

1. 차트가 절대 금액이라 '변동 영향'과 여전히 어긋난다.
2. `변동`이 표지·차트·목록 세 장에 겹쳐 캐러셀에서 같은 화면처럼 보인다.
3. 8자로 길어져 강조 위치가 애매해진다.

이로써 네 장의 역할이 갈린다. 표지는 무슨 일이 있었나, 단가 장은 벤더가 무엇을 올렸나, 차트 장은 지금 어디가 얼마인가, 목록 장은 얼마나 달라졌나다.

**실측:** `플랫폼별 월 사용료`는 76px 본문 제목에서 한 줄에 들어간다. 넘침 없음 [확인].

## 완료 기준

- [x] 모든 장의 문안이 확정됐다
- [x] 구어체가 없다(게이트 통과)
- [x] 사용자가 승인했다 (2026-09-12)
