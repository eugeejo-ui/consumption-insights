# Phase 5 · Task 4: 포트폴리오용 README

| 항목 | 내용 |
|---|---|
| 상태 | **완료** (2026-09-16, `4add92c`). Phase 5 계획의 5-4를 사용자 요청으로 앞당겨 진행했다 |
| 상위 계획 | `docs/phases/phase-5-stabilize.md` 5-4, 규칙 16·17 |
| 문안 정본 | `docs/scripts/readme-script.md` |
| 담당 | C (사용자 확인 2회: **문안 승인**, **push 승인**) |

## 목표

저장소 첫 화면에서 세 가지를 전달한다.

1. 어떤 문제를 자동화했는가
2. 수집 → 탐지 → 검수 → 반영이 어떻게 이어지는가
3. 검수 단계에서 사람이 개입한다는 점과 그 이유

## 건드리는 파일

| 파일 | 동작 |
|---|---|
| `docs/scripts/readme-script.md` | 신규. README 문안 정본 |
| `README.md` | 신규. 문안 승인 뒤 정본을 그대로 옮긴다 |

## 단계

1. Phase 진행계획 6개, Phase 3 과제 기록 11개, 설계 근거, 보안 점검 기록을 읽고 사실을 모은다.
2. README에 실을 숫자를 승인 스냅샷(2026-09-11)에서 다시 계산해 확인한다.
3. 문안을 작성한다(규칙 16 스크립트 단계).
4. `/humanize-korean`으로 점검한다.
5. 사용자 승인을 받는다.
6. `README.md`를 만들고, 링크와 Mermaid 도표가 GitHub에서 그려지는지 확인한다.
7. 개인정보를 검사하고 커밋한다. push는 사용자 승인 뒤에 한다.

## 확인 방법

- 문안의 숫자가 승인 스냅샷 재계산 값과 같다.
- 문안에 이메일, 로컬 경로, 프로젝트 ID, 시트 ID가 없다.
- 공개 문구 규칙(규칙 17)의 고정 용어를 지킨다.

## 위험

| 위험 | 대응 |
|---|---|
| 현재 결과 숫자가 다음 승인 뒤 낡는다 | 기준일(2026-09-11 승인 스냅샷)을 밝히고, 최신 값은 대시보드로 안내한다 |
| 카드(`월 사용료`)와 README(`월 비용`)의 용어가 다르다 | D17의 의도된 불일치다. README는 규칙 17을 따른다 |
| 가설 기호가 처음 보는 독자에게 전달되지 않는다 | D24와 같은 이유로 기호 대신 질문 문장으로 적는다 |

## 완료 기준

- 사용자가 문안을 승인한다.
- `README.md`가 GitHub 첫 화면에 정상적으로 표시된다.

---

# 실행 결과

## 1~2단계: 사실 수집과 숫자 확인 (2026-09-16) [확인]

| 항목 | 값 |
|---|---|
| 승인 스냅샷 단가 | 18개(AWS 4, Azure 4, 수동 가격표 10) |
| 플랫폼 간 월 비용 최대 배율 | 2.28배(소규모 BI, 미국 리전: Redshift $900, Databricks $2,056) |
| 서울 리전 월 비용 증가율 | 11.4%(비정기 대용량 탐색, Redshift) ~ 31.7%(소규모 BI, Databricks) |
| 서울 리전 단가 차이 | -3.8%(Databricks 스토리지) ~ +35.7%(Databricks 서버리스 SQL), 최대·최소 차이 39.5%p |
| 테스트 | 142개 |
| 매일 확인 기록 | 2026-09-12부터 5일, 모두 변화 없음 |

## 3~4단계: 문안 작성과 점검 (2026-09-16)

- 문안: `docs/scripts/readme-script.md` 2절. 12개 절, 8,177자.
- 구성 순서: 요약 → 해결한 문제 4가지 → 전체 흐름(Mermaid + 단계별 통과 조건 표) → 수집·탐지·검수·반영 단계별 설명 → 사람이 맡는 일 → 현재 결과 → 가정과 한계 → 안전장치 → 진행 현황 → 구조·실행·스택·문서.
- 사용자 요청의 `바랭`은 `반영`으로 읽었다. 네 번째 단계 이름을 `반영`으로 적었다.
- 공개 사이트 주소 3개(대시보드, 소개 카드 PDF, 표지 PNG)가 200이다 [확인].
- 반려 사유를 읽는 코멘트 범위는 `publish/revise_request.py`의 `TRUSTED`(소유자·구성원·협업자)와 대조해 `저장소 권한이 있는 사람`으로 적었다 [확인].
- 알림 문장은 `메일이 발송됩니다`가 아니라 `알림을 받습니다`로 적었다. 메일 수신은 계정 설정에 달려 있고 아직 확인하지 않았다 [미확인].
- `/humanize-korean` run `2026-09-16-013`: light 경로, 변경률 0.2%, 게이트 exit 0 수렴, 자체검증 6/6. 스킬 수정 7건과 Claude 추가 수정 3건(한 문장 한 사실 2곳, 맞춤법 1곳)을 반영했다. 상세는 스크립트 3절.
- `python3`는 Windows 스토어 연결이라 동작하지 않는다 [확인]. 스킬 스크립트는 `python`으로 실행했다.

**다음:** 사용자 문안 승인을 기다린다.

## 5~7단계: 승인, README 작성, 커밋 (2026-09-16)

- 사용자가 문안을 승인했다. 확인 요청 3건은 별도 지시가 없어 권고안으로 진행했다(현재 결과는 기준일과 함께 유지, 한국어만, Claude Code 언급 유지).
- 사용자가 `humanize korean 스킬 적용`을 지시했다. 승인본은 이미 run `2026-09-16-013`을 거친 판이다. 같은 문장에 윤문을 다시 돌리면 승인한 문장이 바뀌므로, 승인본을 그대로 옮기고 결정적 게이트로 확인했다.

| 확인 | 결과 [확인] |
|---|---|
| `README.md` = 스크립트 2절 | 같다(8,178자) |
| 게이트(스킬 입력 원문 대비 README) | exit 0 수렴. 문자율 0.3%, 터치율 3.3%(8/242), golden PASS, 서법 소실 0 |
| 스킬 출력 대비 README 차이 | Claude 추가 수정 3건뿐이다 |
| README 상대 링크 대상 | 10개 모두 저장소에 추적된다 |

**계획과 달라진 점:** 6단계의 "Mermaid 도표가 GitHub에서 그려지는지 확인"은 push 뒤에 한다. 로컬에는 Mermaid 렌더러가 없다.

**다음:** push 승인 뒤 GitHub 첫 화면에서 도표와 표가 그려지는지 확인한다.

## push와 GitHub 표시 확인 (2026-09-16)

- 커밋 `4add92c`. 원격에 새 커밋이 없어 리베이스 없이 push했다(`f0dbb7c..4add92c`).
- 문서만 바뀐 push라 게시 워크플로는 실행되지 않았다(정상).

| 확인 | 결과 [확인] |
|---|---|
| GitHub가 렌더링한 README(API) | 표 13개, 제목 h2 13개·h3 8개, 코드 블록 3개, Mermaid 블록 1개, 사이트 링크 3개. 원문 구조와 같다 |
| Mermaid 도표 | 저장소 화면에서 도표 영역(823×180px)이 그려지고 오류 문구가 없다. 캡처에서 노드와 연결 라벨(수집, 탐지, 확인 기록 한 줄, 검수, 머지, 문구 반려)이 보였다 |
| 확인하지 못한 것 | 도표 전체를 한 장에 담는 캡처는 내장 브라우저 창에서 시간 초과로 실패했다. 도표가 다른 출처의 프레임에 그려져 창 캡처가 멈춘 것으로 보인다 [가정] |

**다음 Task에 넘기는 것:** 첫 실제 가격 변동을 승인하면 README 5절(현재 결과)의 숫자가 낡는다. 그때 정본(`docs/scripts/readme-script.md`)부터 고치고 README에 옮긴다.
